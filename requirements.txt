# Core
pydantic>=2.0        # data validation for terms / tokens / AST nodes

# Testing
pytest>=8.0
pytest-cov>=5.0

# Future (Week 2+): computer vision for recognizing PSL identifiers, if input
# is raw image/video rather than a pre-labeled dataset.
# opencv-python
# mediapipe
